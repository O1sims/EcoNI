import { Component } from "preact";
export interface IProps {
    minimized?: boolean;
    onResize?: (dimensions: {
        width: number;
        height: number;
    }) => void;
}
export interface IState {
    unminimize: Function;
}
/** A floating window that supports minimization and resizing  */
export declare class StateVisWindow extends Component<IProps, IState> {
    private deregisterFns;
    el: HTMLElement;
    top: string;
    left: string;
    right: string;
    bottom: string;
    constructor(props: IProps);
    componentWillReceiveProps(nextProps: IProps): void;
    minimize: () => () => void;
    componentWillUnmount(): void;
    componentDidMount(): void;
    /** The uirStateVisContainer class enables resize: both. This function listens for resize events */
    monitorResizeEvents(): void;
    render(): JSX.Element;
}

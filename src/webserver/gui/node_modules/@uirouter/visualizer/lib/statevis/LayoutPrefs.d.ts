import { Component } from 'preact';
import { Renderer } from './interface';
export interface ILayoutPrefsProps {
    onRendererChange: (renderer: Renderer) => void;
}
export interface ILayoutPrefsState {
    renderer: Renderer;
    presetName: string;
}
export declare class LayoutPrefs extends Component<ILayoutPrefsProps, ILayoutPrefsState> {
    state: {
        renderer: Renderer;
        presetName: string;
    };
    componentDidMount(): void;
    handleZoom(event: Event): void;
    handleLayout(event: Event): void;
    render(): JSX.Element;
}
